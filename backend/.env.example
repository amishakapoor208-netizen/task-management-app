# PostgreSQL connection (use with docker-compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/taskapp?schema=public
JWT_SECRET=replace_this_with_a_strong_secret_key_in_production
PORT=4000
